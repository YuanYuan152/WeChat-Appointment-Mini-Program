# 连心心理统一小程序 — 本地开发与使用文档

## 项目概览

本项目是"连心心理"统一微信小程序系统，采用前后端分离架构：

| 组件 | 技术栈 | 端口 | 说明 |
|------|--------|------|------|
| 前端 | Vue 3 + uni-app + TypeScript | 微信开发者工具 | 目标平台 mp-weixin |
| 新后端 | Python FastAPI + SQLAlchemy | 8000 | 统一认证/支付/工作台 API |
| 旧后端 | ASP.NET MVC (C#) | 线上 | 资讯/咨询师列表等历史接口 |
| 数据库 | SQL Server | 1433 | 数据库名 lxxlBuild |

---

## 1. 环境要求

- Node.js >= 18
- pnpm >= 8
- Python >= 3.10
- SQL Server（本地 SQLEXPRESS 或远程实例）
- ODBC Driver 17 for SQL Server
- 微信开发者工具（用于预览小程序）

---

## 2. 快速启动

### 2.1 启动 Python 后端

```bash
cd backend-python

# 首次：创建虚拟环境并安装依赖（已有 venv 可跳过）
python -m venv venv
venv\Scripts\activate        # Windows
pip install -r requirements.txt

# 配置环境变量
copy .env.production .env
# 编辑 .env，填写数据库连接信息（本地开发可保持默认）

# 首次：建表
python create_batch_a_tables.py
python create_counselor_tables.py
python create_assistant_tables.py
python create_ops_tables.py
python create_message_tables.py
python create_patient_tables.py
python create_order_table.py
python create_contact_record_tables.py
python create_counselor_profile_tables.py
python create_refund_exemption_table.py

# 可选：灌入演示数据
python seed_demo_data.py

# 启动（开发模式，热重载）
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

启动成功后：
- API 根路径：http://localhost:8000/
- Swagger 文档：http://localhost:8000/docs
- ReDoc 文档：http://localhost:8000/redoc

### 2.2 启动前端（微信小程序开发模式）

```bash
cd frontend

# 首次：安装依赖
pnpm install

# 启动开发编译（输出到 dist/dev/mp-weixin）
pnpm dev:mp-weixin
```

编译完成后，打开**微信开发者工具**：
1. 选择"导入项目"
2. 目录选择 `frontend/dist/dev/mp-weixin`
3. AppID 填写你的小程序 AppID（或使用测试号）
4. 即可在模拟器中预览

### 2.3 前端其他命令

| 命令 | 说明 |
|------|------|
| `pnpm dev:mp-weixin` | 开发模式编译（watch） |
| `pnpm build:mp-weixin` | 生产构建 |
| `pnpm type-check` | TypeScript 类型检查 |
| `pnpm lint` | ESLint 检查 |
| `pnpm lint:fix` | ESLint 自动修复 |

---

## 3. 项目结构

```
lxxl-main/
├── backend-python/          # Python FastAPI 后端
│   ├── main.py              # 入口，注册所有路由
│   ├── config.py            # 配置（数据库/JWT/微信）
│   ├── database.py          # SQLAlchemy 引擎
│   ├── models.py            # ORM 模型
│   ├── auth.py              # /api/mini/auth/*
│   ├── payment.py           # /api/payment/wechat/*
│   ├── upload.py            # /api/upload/*
│   ├── common.py            # /api/mini/common/*
│   ├── patient.py           # /api/mini/patient/*
│   ├── counselor.py         # /api/mini/counselor/*
│   ├── assistant.py         # /api/mini/assistant/*
│   ├── ops.py               # /api/mini/ops/*
│   ├── message.py           # /api/mini/message/*
│   ├── admin.py             # /api/mini/admin/*
│   └── .env.production      # 环境变量模板
├── frontend/                # uni-app 前端
│   ├── src/
│   │   ├── pages/           # 页面（按分包组织）
│   │   ├── apis/            # API 调用层
│   │   ├── store/           # Pinia 状态管理
│   │   ├── utils/           # 工具函数
│   │   ├── config/          # 配置（API 端点）
│   │   ├── components/      # 公共组件
│   │   ├── types/           # TypeScript 类型
│   │   └── pages.json       # 路由与分包配置
│   ├── .env                 # 开发环境变量
│   └── .env.production      # 生产环境变量
├── lxxl/                    # 旧 C# 后端（保留兼容）
└── 升级方案.md              # 系统升级方案文档
```

---

## 4. 小程序分包与角色

### 4.1 分包结构

| 分包 | 路径前缀 | 目标角色 |
|------|---------|---------|
| 主包 | `pages/` | 游客、患者 |
| patient | `pages/patient/` | 患者 |
| consultation | `pages/consultation/` | 患者 |
| counselor | `pages/counselor/` | 咨询师 |
| assistant | `pages/assistant/` | 咨询助理 |
| ops | `pages/ops/` | 市场运营、管理员 |
| admin-webview | `pages/admin-webview/` | 管理员 |

### 4.2 底部 Tab

| Tab | 页面 | 说明 |
|-----|------|------|
| 首页 | pages/index/index | 内容门户 |
| 活动 | pages/activity/list | 活动招募 |
| 咨询 | pages/consultant/list | 咨询师列表 |
| 工作台 | pages/workbench/index | 按角色路由到对应工作台 |
| 我的 | pages/user/profile | 个人中心 |

### 4.3 角色体系

| 角色 | 说明 | 工作台入口 |
|------|------|-----------|
| Patient | 来访者（默认） | 工作台显示「暂不对来访者开放」提示 |
| Counselor | 咨询师 | 咨询师工作台（周历 + 挂课） |
| Assistant | 咨询助理 | 助理工作台 |
| Ops | 市场运营 | 运营工作台 |
| Admin | 管理员 | 运营工作台 + Web 重后台 |

登录后点击「工作台」Tab：
- **来访者**：停留提示页「当前工作台界面暂时不对来访者开放哦」，可跳转咨询/我的
- **咨询师/助理/运营/管理员**：按当前活跃角色 `redirectTo` 对应工作台分包页面

---

## 5. API 路由总览

### 5.1 认证（/api/mini/auth）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/mini/auth/login | 微信登录（code 换 JWT） |
| POST | /api/mini/auth/bind-mobile | 绑定手机号 |
| GET | /api/mini/auth/me | 获取当前用户信息与角色 |
| POST | /api/mini/auth/switch-role | 切换活跃角色 |
| DELETE | /api/mini/auth/account | 注销账号 |

### 5.2 支付（/api/payment/wechat）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/payment/wechat/create | 统一下单，返回支付参数 |
| POST | /api/payment/wechat/simulate-pay | 开发环境一键模拟支付并预约成功 |
| POST | /api/payment/wechat/callback | 微信支付异步回调 |

### 5.3 公共内容（/api/mini/common）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/mini/common/banners | 首页 Banner |
| GET | /api/mini/common/articles | 文章列表 |
| GET | /api/mini/common/articles/:id | 文章详情 |
| GET | /api/mini/common/counselors | 咨询师公开列表 |
| GET | /api/mini/common/counselors/:id | 咨询师详情 + 可约时段 |
| GET | /api/mini/common/search | 全局搜索 |

### 5.4 患者（/api/mini/patient）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/mini/patient/orders | 我的订单列表 |
| GET | /api/mini/patient/orders/:id | 订单详情 |
| GET | /api/mini/patient/consultations | 咨询记录 |
| POST | /api/mini/patient/consultations/:id/cancel | 来访者取消预约（>24h 退款） |
| GET | /api/mini/patient/me | 患者资料 |
| PUT | /api/mini/patient/me | 更新资料 |
| GET/PUT | /api/mini/patient/registration | 心理登记表 |

### 5.5 咨询师工作台（/api/mini/counselor）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | /api/mini/counselor/schedules | 挂课管理（AVAILABLE=来访者可见可约） |
| GET | /api/mini/counselor/schedules/calendar | 未来一周周历（已挂课/已预约/已完成） |
| GET | /api/mini/counselor/consultations | 咨询单列表 |
| PUT | /api/mini/counselor/consultations/:id | 更新咨询单状态 |
| GET/POST/PUT | /api/mini/counselor/case-records | 个案记录 |
| GET/PUT | /api/mini/counselor/profile | 咨询师资料 |
| GET | /api/mini/counselor/stats | 统计数据 |

### 5.6 助理工作台（/api/mini/assistant）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/mini/assistant/tasks | 待处理任务 |
| PUT | /api/mini/assistant/tasks/:id | 更新任务状态 |
| GET | /api/mini/assistant/risk-alerts | 风险提醒 |
| PUT | /api/mini/assistant/risk-alerts/:id | 标记已处理 |
| GET | /api/mini/assistant/schedule-overview | 排期协调总览 |
| GET/POST | /api/mini/assistant/contact-records | 联系记录 |

### 5.7 运营（/api/mini/ops）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | /api/mini/ops/banners | Banner 管理 |
| GET/POST/DELETE | /api/mini/ops/activities | 活动管理 |
| GET/POST/DELETE | /api/mini/ops/articles | 文章管理 |
| GET | /api/mini/ops/users | 用户列表 |
| GET | /api/mini/ops/dashboard | 运营看板数据 |

### 5.8 管理员（/api/mini/admin）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/mini/admin/users | 用户列表 |
| POST/DELETE | /api/mini/admin/users/:uid/roles | 绑定/解绑角色 |

### 5.9 消息（/api/mini/message）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/mini/message/list | 消息列表 |
| GET | /api/mini/message/unread-count | 未读数 |
| PUT | /api/mini/message/:id/read | 标记已读 |
| POST | /api/mini/message/subscribe | 订阅消息注册 |
| GET | /api/mini/message/templates | 获取模板 ID |

### 5.10 文件上传（/api/upload）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/upload/file | 上传文件，返回 URL |

---

## 6. 环境变量说明

### 6.1 后端 (.env)

| 变量 | 说明 | 默认值 |
|------|------|--------|
| DB_SERVER | 数据库地址 | .\SQLEXPRESS |
| DB_PORT | 数据库端口 | 1433 |
| DB_NAME | 数据库名 | lxxlBuild |
| DB_USER | 数据库用户 | （空=集成认证） |
| DB_PASSWORD | 数据库密码 | （空=集成认证） |
| DB_TRUSTED_CONNECTION | 是否 Windows 集成认证 | true |
| WECHAT_APPID | 小程序 AppID | （需填写） |
| WECHAT_SECRET | 小程序 Secret | （需填写） |
| WECHAT_PAY_MCH_ID | 微信支付商户号 | （需填写） |
| WECHAT_PAY_KEY | 微信支付 API Key | （需填写） |
| JWT_SECRET | JWT 签名密钥 | （需修改） |
| BASE_URL | 服务对外 URL | http://localhost:8000 |

### 6.2 前端 (.env)

| 变量 | 说明 | 开发默认值 |
|------|------|-----------|
| VITE_API_BASE_URL | 旧 C# 后端地址 | https://www.ji-psy.com |
| VITE_API_V2_BASE_URL | 新 Python 后端地址 | http://localhost:8000 |
| VITE_USE_BOOKING_MOCK | 是否使用预约 Mock 数据 | false |
| VITE_ADMIN_WEB_URL | 运营后台嵌入地址 | http://localhost:8000/docs |
| VITE_API_TIMEOUT | 请求超时(ms) | 10000 |

---

## 7. 核心业务流程

### 7.1 用户登录

```
用户点击"微信一键登录"
  → uni.login() 获取 code
  → POST /api/mini/auth/login { code }
  → 后端 code2Session 换 openid
  → 返回 JWT token + is_new_user
  → 可选：getPhoneNumber 获取 phoneCode
  → POST /api/mini/auth/bind-mobile { phoneCode }
```

### 7.2 挂课与预约联动

```
咨询师工作台 → 新建挂课 POST /api/mini/counselor/schedules
  → 时段 Status=AVAILABLE（周历显示「已挂课」🟢）
来访者咨询详情 → 仅展示 AVAILABLE 时段可点；BOOKED 灰色不可选
  → 确认支付 POST /api/payment/wechat/simulate-pay（开发）或 create + 微信支付
  → 支付成功：时段 BOOKED + 创建咨询单（咨询师周历显示「已预约」🔵）
来访者取消预约 POST /api/mini/patient/consultations/:id/cancel
  → BOOKED 释放为 AVAILABLE，其他来访者可再约
咨询师完成咨询 PUT consultations/:id { status: DONE }
  → 周历显示「已完成」✅
```

### 7.3 角色切换

```
工作台 Tab → GET /api/mini/auth/me
  → activeRole=Patient → 来访者提示页
  → activeRole=Counselor/Assistant/Ops/Admin → redirectTo 对应工作台
开发环境可在登录页 DevRolePicker 切换 seed 账号角色后重新登录
```

### 7.4 开发测试账号（seed_demo_data.py）

| 角色 | 登录方式 | 说明 |
|------|----------|------|
| 来访者 | DevRolePicker 选「来访者」 | 工作台拦截；可预约咨询师 |
| 咨询师 | 选「咨询师」王婉清等 | 工作台周历、新建挂课 |
| 助理/运营/管理员 | 对应 DevRolePicker | 各自工作台 |

---

## 8. 开发注意事项

1. **双后端架构**：前端同时对接旧 C# 后端和新 Python 后端。新功能一律走 `httpV2`（Python 后端），旧接口通过 `http` 访问。

2. **路由守卫**：counselor/assistant/ops 分包页面有角色权限拦截，未登录或角色不匹配会被拦截。

3. **Token 管理**：JWT 存储在 `uni.setStorageSync('token')`，所有请求自动注入 `Authorization: Bearer` 头。

4. **文件上传**：使用 `httpV2.upload(url, filePath, name)` 方法，底层调用 `uni.uploadFile`。

5. **小程序包大小**：主包控制在 2MB 以内，分包各自独立。`preloadRule` 配置了首页预加载 patient 分包。

6. **Mock 模式**：设置 `VITE_USE_BOOKING_MOCK=true` 可在无后端时演示预约流程。

---

## 9. 生产部署

### 后端

```bash
cd backend-python
# 编辑 .env 填写生产配置
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

建议使用 Nginx 反向代理 + HTTPS。

### 前端

```bash
cd frontend
# 编辑 .env.production 填写生产 API 地址
pnpm build:mp-weixin
```

构建产物在 `dist/build/mp-weixin`，通过微信开发者工具上传发布。

---

## 10. 常见问题

**Q: 后端启动报 pyodbc 连接失败？**
A: 确认已安装 ODBC Driver 17 for SQL Server，且 SQL Server 服务已启动。本地开发可设置 `DB_TRUSTED_CONNECTION=true` 使用 Windows 集成认证。

**Q: 前端编译报 uni_modules 类型错误？**
A: 这是第三方组件的已知问题，不影响构建。`tsconfig.json` 已排除 `src/uni_modules`。

**Q: 微信开发者工具中请求失败？**
A: 在微信开发者工具中勾选"不校验合法域名"（开发阶段），确保后端 `http://localhost:8000` 可访问。

**Q: 如何测试支付？**
A: 本地开发时，后端返回 mock 支付参数（appId 以 `wx_mock` 开头），前端会跳过真实支付直接完成。生产环境需配置真实微信支付商户号。
