# 连心心理小程序升级功能测试文档

本文档用于指导本地启动前后端，并验证本次升级已完成的患者端链路、咨询师工作台、助理工作台、运营后台、消息通知体系和角色切换功能。

## 1. 环境要求

### 1.1 后端

- Windows 10/11
- Python 3.10+，当前项目使用 `backend-python/venv`
- SQL Server Express
- ODBC Driver 17 for SQL Server
- 本地数据库：`lxxlBuild`
- 后端端口：`8000`

### 1.2 前端

- Node.js 18+
- pnpm 8+
- 微信开发者工具
- 前端目录：`frontend`
- 新后端地址配置在 `frontend/.env`：

```env
VITE_API_V2_BASE_URL=http://localhost:8000
```

## 2. 后端启动

进入后端目录：

```powershell
cd d:\data\extra\lxxl-main\backend-python
```

如首次运行，安装依赖：

```powershell
.\venv\Scripts\pip.exe install -r requirements.txt
```

数据库连接配置在：

```text
backend-python\config.py
backend-python\.env
backend-python\.env.example
```

本地默认使用 Windows 集成认证：

```env
DB_SERVER=.\SQLEXPRESS
DB_PORT=1433
DB_NAME=lxxlBuild
DB_TRUSTED_CONNECTION=true
```

如果使用 SQL Server 用户名密码认证，例如远程服务器或 `sa` 账号，请在 `backend-python\.env` 中配置：

```env
DB_SERVER=192.168.1.100
DB_PORT=1433
DB_NAME=lxxlBuild
DB_USER=sa
DB_PASSWORD=你的密码
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_TRUSTED_CONNECTION=false
DB_TRUST_SERVER_CERTIFICATE=true
```

首次初始化数据库时，先用 SQL Server Management Studio 或其他 SQL Server 客户端执行：

```text
d:\data\extra\lxxl-main\lxxl\App_Data\01_Create_MiniProgram_Tables.sql
```

该 SQL 文件用于创建统一账号、角色绑定、登录态、订单等基础表。

然后在 PowerShell 中继续执行这些 Python 建表/升级脚本：

```powershell
.\venv\Scripts\python.exe create_counselor_tables.py
.\venv\Scripts\python.exe create_assistant_tables.py
.\venv\Scripts\python.exe create_ops_tables.py
.\venv\Scripts\python.exe create_batch_a_tables.py
.\venv\Scripts\python.exe alter_app_account.py
.\venv\Scripts\python.exe create_message_tables.py
.\venv\Scripts\python.exe create_patient_tables.py
.\venv\Scripts\python.exe create_counselor_profile_tables.py
.\venv\Scripts\python.exe create_contact_record_tables.py
```

启动 FastAPI：

```powershell
.\venv\Scripts\uvicorn.exe main:app --host 0.0.0.0 --port 8000 --reload
```

启动成功后访问：

- API 首页：`http://localhost:8000/`
- Swagger 文档：`http://localhost:8000/docs`
- OpenAPI：`http://localhost:8000/openapi.json`

## 3. 后端自动化冒烟测试

后端启动后运行：

```powershell
cd d:\data\extra\lxxl-main\backend-python
.\venv\Scripts\python.exe test_core_flow.py
```

预期看到：

```text
All tests PASSED!
```

当前脚本覆盖：

- 健康检查
- 小程序 mock 登录
- JWT 鉴权
- 患者订单
- 无角色访问咨询师/助理接口应返回 403
- 运营公开 Banner/活动接口
- mock 支付下单

建议后续可把新增的消息、登记表、角色切换、运营文章等接口继续补充到该脚本。

## 4. 前端启动

进入前端目录：

```powershell
cd d:\data\extra\lxxl-main\frontend
```

安装依赖：

```powershell
pnpm install
```

### 4.1 H5 调试

```powershell
pnpm dev:h5
```

浏览器打开控制台输出的本地地址。

### 4.2 微信小程序调试

```powershell
pnpm dev:mp-weixin
```

然后用微信开发者工具导入编译产物目录。通常为：

```text
frontend\dist\dev\mp-weixin
```

如果实际输出目录不同，以终端输出为准。

## 5. 测试账号与角色准备

本项目目前本地开发使用 mock 登录：

- 前端调用 `uni.login`
- 后端未配置真实微信凭证时，会生成 `mock_openid_xxx`
- 登录后会创建 `AppAccount`

咨询师、助理、运营、管理员页面需要角色绑定。可通过数据库临时授权。

### 5.1 查询当前账号

在 Swagger 或前端登录后调用：

```http
GET /api/mini/auth/me
```

记下返回的 `id`。

### 5.2 手动插入角色

在 SQL Server 中执行：

```sql
-- 将 1 替换为实际 AppAccount.Id
INSERT INTO AppRoleBinding(AccountId, RoleType, TargetId, CreatedAt)
VALUES (1, 'Counselor', NULL, GETDATE());

INSERT INTO AppRoleBinding(AccountId, RoleType, TargetId, CreatedAt)
VALUES (1, 'Assistant', NULL, GETDATE());

INSERT INTO AppRoleBinding(AccountId, RoleType, TargetId, CreatedAt)
VALUES (1, 'Ops', NULL, GETDATE());

INSERT INTO AppRoleBinding(AccountId, RoleType, TargetId, CreatedAt)
VALUES (1, 'Admin', NULL, GETDATE());
```

可用角色：

- `Patient`
- `Counselor`
- `Assistant`
- `Ops`
- `Admin`

## 6. 角色切换测试

接口：

```http
POST /api/mini/auth/switch-role
Authorization: Bearer <token>
Content-Type: application/json

{
  "role": "Counselor"
}
```

预期：

```json
{
  "message": "角色已切换",
  "activeRole": "Counselor"
}
```

前端路径：

```text
pages/workbench/index
```

底部 Tab 的“工作台”会进入该中转页，并根据当前角色跳转：

- `Counselor` → 咨询师工作台
- `Assistant` → 助理工作台
- `Ops` → Banner 管理
- `Admin` → Web 后台容器
- 无业务角色 → 我的页面

## 7. 患者端测试

### 7.1 患者资料

前端页面：

```text
pages/patient/profile/edit
```

接口：

```http
GET /api/mini/patient/me
PUT /api/mini/patient/me
```

测试点：

- 昵称
- 真实姓名
- 性别
- 生日
- 紧急联系人
- 紧急联系人电话

### 7.2 完整版心理登记表

前端页面：

```text
pages/patient/registration/edit
```

接口：

```http
GET /api/mini/patient/registration
PUT /api/mini/patient/registration
```

测试点：

- 基础信息保存
- 主诉保存
- PHQ-9 量表 9 项评分
- GAD-7 量表 7 项评分
- 既往诊断、治疗经历、用药史
- 家庭关系、家族精神心理史
- 自伤风险、咨询目标
- 保存后再次进入页面能回显

### 7.3 消息中心

前端页面：

```text
pages/patient/messages/index
```

接口：

```http
GET /api/mini/message/list
GET /api/mini/message/unread-count
PUT /api/mini/message/{message_id}/read
POST /api/mini/message/subscribe
```

测试点：

- 未读消息数量
- 全部/未读筛选
- 点击消息后标记已读
- 订阅消息当前为 mock 日志模式

可用 Swagger 创建一条测试消息：

```http
POST /api/mini/message/system
```

请求体：

```json
{
  "account_id": 1,
  "type": "SYSTEM",
  "title": "测试消息",
  "content": "这是一条测试站内消息"
}
```

## 8. 咨询师工作台测试

测试前账号需绑定 `Counselor` 角色。

### 8.1 工作台首页

前端页面：

```text
pages/counselor/workbench/index
```

测试点：

- 今日排班
- 待确认咨询单
- 确认/拒绝咨询单

### 8.2 排班管理

前端页面：

```text
pages/counselor/schedule/index
```

接口：

```http
GET /api/mini/counselor/schedules
POST /api/mini/counselor/schedules
PUT /api/mini/counselor/schedules/{id}
```

测试点：

- 新增排班
- 取消排班
- 列表刷新

### 8.3 咨询记录与个案记录

前端页面：

```text
pages/counselor/consultations/index
pages/counselor/case-record/edit
```

接口：

```http
GET /api/mini/counselor/consultations
PUT /api/mini/counselor/consultations/{id}
GET /api/mini/counselor/case-records
POST /api/mini/counselor/case-records
PUT /api/mini/counselor/case-records/{id}
```

测试点：

- 咨询单状态切换：已确认、进行中、已完成、已取消
- SOAP 个案记录保存

### 8.4 咨询师个人资料

前端页面：

```text
pages/counselor/profile/edit
```

接口：

```http
GET /api/mini/counselor/profile
PUT /api/mini/counselor/profile
```

测试点：

- 姓名、头像、头衔
- 咨询流派
- 个人介绍
- 职业经历
- 资质证书
- 咨询费用

### 8.5 统计看板

前端页面：

```text
pages/counselor/stats/index
```

接口：

```http
GET /api/mini/counselor/stats
```

测试点：

- 本月咨询数
- 待确认数量
- 已完成数量
- 预估收入

## 9. 助理工作台测试

测试前账号需绑定 `Assistant` 角色。

### 9.1 助理首页

前端页面：

```text
pages/assistant/workbench/index
```

测试点：

- 待处理任务数量
- 风险提醒数量
- 本周排班数量

### 9.2 待处理任务

前端页面：

```text
pages/assistant/tasks/index
```

接口：

```http
GET /api/mini/assistant/tasks
POST /api/mini/assistant/tasks
PUT /api/mini/assistant/tasks/{id}
```

测试点：

- 状态筛选
- 标记完成

### 9.3 风险提醒

前端页面：

```text
pages/assistant/risk/index
```

接口：

```http
GET /api/mini/assistant/risk-alerts
POST /api/mini/assistant/risk-alerts
PUT /api/mini/assistant/risk-alerts/{id}
```

测试点：

- 风险等级展示
- 标记已处理

### 9.4 联系记录

前端页面：

```text
pages/assistant/contact/index
```

接口：

```http
GET /api/mini/assistant/contact-records
POST /api/mini/assistant/contact-records
```

测试点：

- 新增联系记录
- 电话/微信/线下/其他方式
- 列表回显

## 10. 运营后台测试

测试前账号需绑定 `Ops` 或 `Admin` 角色。

### 10.1 Banner 管理

前端页面：

```text
pages/ops/banner/index
```

接口：

```http
GET /api/mini/ops/banners
POST /api/mini/ops/banners
PUT /api/mini/ops/banners/{id}
DELETE /api/mini/ops/banners/{id}
```

测试点：

- 新增 Banner
- 上传/填写图片 URL
- 启用/停用
- 删除

### 10.2 文章管理

前端页面：

```text
pages/ops/articles/index
```

接口：

```http
GET /api/mini/ops/articles
POST /api/mini/ops/articles
PUT /api/mini/ops/articles/{id}
DELETE /api/mini/ops/articles/{id}
```

测试点：

- 发布文章
- 前台公共搜索可搜到文章
- 删除文章

### 10.3 活动管理

前端页面：

```text
pages/ops/activities/index
```

接口：

```http
GET /api/mini/ops/activities
POST /api/mini/ops/activities
PUT /api/mini/ops/activities/{id}
DELETE /api/mini/ops/activities/{id}
```

测试点：

- 发布活动/公告
- 删除活动/公告
- 前台活动列表可读取公开活动接口

### 10.4 运营数据看板

前端页面：

```text
pages/ops/dashboard/index
```

接口：

```http
GET /api/mini/ops/dashboard
```

测试点：

- 用户数
- 订单数
- 已支付订单
- 支付金额
- 文章数
- 活动数

## 11. 管理员轻控制台测试

测试前账号需绑定 `Admin` 角色。

前端页面：

```text
pages/ops/admin-roles/index
```

接口：

```http
GET /api/mini/admin/users
POST /api/mini/admin/users/{user_id}/roles
DELETE /api/mini/admin/users/{user_id}/roles/{role}
```

测试点：

- 查看用户列表
- 给用户绑定 `Counselor`、`Assistant`、`Ops`、`Admin`
- 解绑角色
- 被绑定角色的账号重新进入“工作台”后能跳转到对应工作台

## 12. 公共搜索测试

前端页面：

```text
pages/search/index
```

接口：

```http
GET /api/mini/common/search?q=关键词
```

测试点：

- 搜索咨询师
- 搜索文章
- 搜索活动
- 结果点击跳转

## 13. 支付链路测试

当前本地没有真实微信支付配置时，后端返回 mock 支付参数。

接口：

```http
POST /api/payment/wechat/create
```

请求示例：

```json
{
  "slot_id": 1,
  "total_fee": 29900,
  "description": "心理咨询预约"
}
```

预期返回：

```json
{
  "out_trade_no": "LXXL...",
  "pay_params": {
    "appId": "wx_mock_appid",
    "timeStamp": "...",
    "nonceStr": "...",
    "package": "prepay_id=mock_...",
    "signType": "MD5",
    "paySign": "MOCK_SIGN_..."
  }
}
```

正式上线前需配置：

- `WECHAT_APPID`
- `WECHAT_SECRET`
- `WECHAT_PAY_MCH_ID`
- `WECHAT_PAY_KEY`
- `WECHAT_PAY_NOTIFY_URL`

## 14. 常见问题

### 14.1 后端接口 401

说明没有登录或 token 丢失。重新登录，或在 Swagger 里使用 `Authorization: Bearer <token>`。

### 14.2 后端接口 403

说明当前账号没有对应角色。给账号插入 `AppRoleBinding`。

### 14.3 小程序访问 `localhost:8000` 失败

微信开发者工具中需要开启：

```text
详情 -> 本地设置 -> 不校验合法域名、web-view、TLS 版本以及 HTTPS 证书
```

真机测试时不能用 `localhost`，需要换成本机局域网 IP 或公网 HTTPS 域名。

### 14.4 订阅消息没有真实推送

当前按测试策略使用 mock 模板 ID，只记录到 `AppMessageLog`。上线前需要在微信公众平台创建订阅消息模板，并写入 `AppSubscribeTemplate`。

### 14.5 旧内容/咨询师为空

当前 `lxxlBuild` 是新库，不包含旧 `T_Content` / `T_Doctor` 表。公共接口已经预留双源读取逻辑，将来接入旧库或迁移旧表后可展示旧内容。

## 15. 推荐测试顺序

1. 启动后端并打开 Swagger。
2. 运行 `test_core_flow.py`。
3. 前端启动 H5 或微信小程序。
4. 登录生成账号。
5. 用 SQL 给账号绑定 `Admin`。
6. 进入 `pages/ops/admin-roles/index` 给自己绑定 `Counselor`、`Assistant`、`Ops`。
7. 测试“工作台”角色跳转。
8. 测试患者登记表与消息中心。
9. 测试咨询师排班、资料、统计。
10. 测试助理任务、风险、联系记录。
11. 测试运营 Banner、文章、活动、看板。
12. 测试管理员角色绑定/解绑。
