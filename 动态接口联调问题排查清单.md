# 动态接口联调问题排查清单

生成时间：2026-05-19
最近复查：2026-05-19 01:14

## 当前结论

前端已关闭 `VITE_USE_BOOKING_MOCK=false`，并且本次编辑范围内前端 lint 无报错；后端 `uvicorn` 可以启动，`/openapi.json` 可以访问，说明 FastAPI 应用和路由注册基本正常。

当前主要阻塞已从“数据库名配置不一致”变为“当前连接到的 `lxxlBuild` 表结构不完整”。最新复查显示 FastAPI 当前确实连接到了 `localhost:1433 / lxxlBuild / sa`，但该库里只有 `AppAccount / AppLoginSession / AppRoleBinding` 三张 App 表，且 `AppAccount` 仍缺少 `ActiveRole / RealName / Gender / Birthday / EmergencyContact / EmergencyPhone` 等升级字段。

这会导致：

- `seed_demo_data.py` 仍无法写入演示数据。
- `/api/mini/auth/login` 返回 500。
- `/api/mini/common/banners` 和 `/api/mini/ops/activities` 返回 500。
- `/api/mini/common/counselors` 返回 200 但空列表。

## 最新复查结果

### 当前实际生效配置

```text
DB_SERVER= localhost
DB_PORT= 1433
DB_NAME= lxxlBuild
DB_USER= sa
DB_TRUSTED_CONNECTION= False
DB_DRIVER= ODBC Driver 17 for SQL Server
```

### 当前实际连接数据库

```text
server= f8e9c1c9daf3
db= lxxlBuild
schema= dbo
```

### 当前实际存在的 App 表

```text
AppAccount
AppLoginSession
AppRoleBinding
```

### 当前 AppAccount 列

```text
Id
UnionId
OpenId
Mobile
Nickname
AvatarUrl
CreatedAt
UpdatedAt
```

缺少：

```text
ActiveRole
RealName
Gender
Birthday
EmergencyContact
EmergencyPhone
```

### 当前核心接口状态

```text
/                                      200
/openapi.json                          200
/api/mini/common/banners               500
/api/mini/common/counselors            200 {"total":0,"items":[]}
/api/mini/ops/activities               500
/api/mini/auth/login                   500
```

### seed 当前失败原因

```text
Invalid column name 'ActiveRole'
Invalid column name 'RealName'
Invalid column name 'Gender'
Invalid column name 'Birthday'
Invalid column name 'EmergencyContact'
Invalid column name 'EmergencyPhone'
```

这说明当前连接目标里的 `AppAccount` 没有执行过 `alter_app_account.py` 的升级效果，或者该脚本执行到了另一个 SQL Server 实例/另一个认证上下文。

## 已确认问题

### 1. 数据库结构不完整（最新主问题）

现象：

- `lxxlBuild` 已经连上。
- 但缺少 `AppBanner / AppActivity / AppArticle / AppCounselorProfile / AppSchedule / AppMessage / AppTask` 等新表。
- `AppAccount` 缺少升级字段。

影响：

- seed 写入失败。
- 登录、首页、活动、Banner、工作台等接口返回 500。

建议优先处理：

```powershell
cd d:\data\extra\lxxl-main\backend-python
.\venv\Scripts\python.exe alter_app_account.py
.\venv\Scripts\python.exe create_ops_tables.py
.\venv\Scripts\python.exe create_batch_a_tables.py
.\venv\Scripts\python.exe create_message_tables.py
.\venv\Scripts\python.exe create_patient_tables.py
.\venv\Scripts\python.exe create_counselor_tables.py
.\venv\Scripts\python.exe create_assistant_tables.py
.\venv\Scripts\python.exe create_counselor_profile_tables.py
.\venv\Scripts\python.exe create_contact_record_tables.py
```

注意：这些脚本当前仍写死 `SERVER=.\SQLEXPRESS;DATABASE=lxxlBuild;Trusted_Connection=yes;`。如果你现在 FastAPI 使用的是 `localhost,1433 + sa`，脚本可能仍然打到不同实例或不同认证上下文。执行后必须用下面命令确认 FastAPI 当前连接里真的出现了新表。

```powershell
cd d:\data\extra\lxxl-main
& "backend-python\venv\Scripts\python.exe" -c "import sys; sys.path.insert(0, 'backend-python'); from database import engine; from sqlalchemy import inspect; insp=inspect(engine); print([t for t in insp.get_table_names() if t.startswith('App')])"
```

更稳妥的做法：

- 直接运行 `backend-python/init_db.py`，让 SQLAlchemy 按当前 `config.py` 连接创建缺失表。
- 然后再运行 `alter_app_account.py`，补齐旧表缺失字段。

```powershell
cd d:\data\extra\lxxl-main
& "backend-python\venv\Scripts\python.exe" "backend-python\init_db.py"
& "backend-python\venv\Scripts\python.exe" "backend-python\alter_app_account.py"
& "backend-python\venv\Scripts\python.exe" "backend-python\seed_demo_data.py"
```

如果 `alter_app_account.py` 仍然没有作用，需要把它的连接串也改成读取 `config.py`，确保和 FastAPI 同一个连接目标。

### 2. 数据库名配置不一致（历史问题，当前已基本修正）

现象：

```text
DB_NAME= dbo
```

但历史建表脚本和 `.env.production` 默认都是：

```text
DB_NAME=lxxlBuild
SERVER=.\SQLEXPRESS
Trusted_Connection=yes
```

影响：

- `seed_demo_data.py` 写入失败。
- 首页 Banner、活动、文章、咨询师资料可能读不到正确库。
- 已经执行过的建表脚本可能创建在 `lxxlBuild`，而 FastAPI 正在查 `dbo`。

建议排查：

```powershell
cd d:\data\extra\lxxl-main
python -c "import sys; sys.path.insert(0, r'backend-python'); from config import settings; print(settings.DB_SERVER, settings.DB_PORT, settings.DB_NAME, settings.DB_USER, settings.DB_TRUSTED_CONNECTION)"
```

优先建议：

- 本地开发如果使用 SQL Server Express，优先统一为 `DB_SERVER=.\SQLEXPRESS`、`DB_NAME=lxxlBuild`、`DB_TRUSTED_CONNECTION=true`。
- 如果坚持使用用户名密码方式，需要确认 `sa` 已启用、密码正确、并且有访问目标数据库的权限。

### 3. `sa` 登录失败或无目标库权限（历史问题，当前已基本修正）

seed 执行失败核心错误：

```text
Login failed for user 'sa'
Cannot open database "dbo" requested by the login
```

影响：

- `backend-python/seed_demo_data.py` 无法写入演示数据。
- 支付、登录、工作台等所有依赖数据库的接口都有潜在失败风险。

建议排查：

```powershell
cd d:\data\extra\lxxl-main
& "backend-python\venv\Scripts\python.exe" "backend-python\seed_demo_data.py"
```

如果仍失败，请在 SQL Server Management Studio 中验证：

- SQL Server 是否允许 SQL Server 身份验证。
- `sa` 是否启用。
- `sa` 默认数据库是否存在且可访问。
- 目标数据库到底是 `dbo` 还是 `lxxlBuild`。

### 4. 公共接口部分返回 500

实测结果：

```text
/openapi.json                         200
/api/mini/common/banners              500
/api/mini/common/counselors           200 {"total":0,"items":[]}
/api/mini/ops/activities              500
```

判断：

- 路由已注册。
- 数据库连接可部分建立，但当前库/表/权限/字段可能不匹配。
- `counselors` 返回 200 但为空，说明没有可用 `AppCounselorProfile` 或旧 `T_Doctor` 数据。

建议排查：

```powershell
python -c "import requests; base='http://127.0.0.1:8000'; paths=['/openapi.json','/api/mini/common/banners','/api/mini/common/counselors','/api/mini/ops/activities']; [print(p, requests.get(base+p, timeout=5).status_code, requests.get(base+p, timeout=5).text[:300]) for p in paths]"
```

同时查看 `uvicorn` 终端里 500 对应的 SQL 报错，通常会指向缺表、缺列或连接库错误。

### 5. 初始化脚本连接串仍写死为集成认证

这些脚本仍使用：

```text
SERVER=.\SQLEXPRESS;DATABASE=lxxlBuild;Trusted_Connection=yes;
```

涉及脚本包括：

- `backend-python/create_counselor_tables.py`
- `backend-python/create_assistant_tables.py`
- `backend-python/create_ops_tables.py`
- `backend-python/create_batch_a_tables.py`
- `backend-python/create_message_tables.py`
- `backend-python/create_patient_tables.py`
- `backend-python/create_counselor_profile_tables.py`
- `backend-python/create_contact_record_tables.py`
- `backend-python/alter_app_account.py`

影响：

- 建表脚本和 FastAPI 运行时可能操作不同数据库。
- 你看到“表创建成功”，但接口依然报缺表或空数据。

建议：

- 先统一 `config.py` 和建表脚本目标数据库。
- 后续可以把这些脚本也改为读取 `config.py`，避免再次漂移。

## 前端侧疑似问题

### 5. 图片 URL 可能被拼到旧 C# 域名

`frontend/src/utils/image.ts` 当前对 `/static/...` 会拼接旧后端：

```text
VITE_API_BASE_URL=https://www.ji-psy.com
```

但新 seed 数据使用的是：

```text
/static/images/slide11.png
/static/images/huodong11.png
/static/images/zixunshi11.png
```

影响：

- 首页 Banner、活动图、咨询师头像可能请求到 `https://www.ji-psy.com/static/...`，而不是前端本地静态资源或 FastAPI 静态资源。

建议排查：

- 打开浏览器 Network，看图片实际请求域名。
- 若显示 404，可把 `/static/...` 保持原样，不再拼旧域名；或改成由 `VITE_API_V2_BASE_URL` 拼接后端静态资源。

### 6. 仍有 mock 逻辑残留，但当前不会启用

已确认存在残留：

- `frontend/src/pages/consultant/detail.vue` 仍保留 `isBookingDemoMock()` 分支。
- `frontend/src/pages/index/index.vue` 在接口失败时仍会 fallback 到 `loadMockData()`。
- `frontend/src/pages/consultant/list.vue` 的筛选项仍来自 `getMockConsultantFilterMetaResponse()`。

当前影响：

- 因为 `.env` 已设置 `VITE_USE_BOOKING_MOCK=false`，咨询师详情页 mock 分支不会走。
- 首页如果接口失败，会显示本地模拟数据，可能掩盖真实接口错误。
- 咨询师筛选项目前只是前端静态选项，不影响列表接口，但筛选参数在新后端暂未完整支持。

建议：

- 联调阶段建议保留 fallback，但测试时要看控制台是否出现“网络连接失败，已加载本地数据”。
- 上线前再决定是否移除首页 fallback。

### 7. 仍有旧 C# 接口常量和测试页引用

旧接口仍在：

- `frontend/src/config/api.ts`
- `frontend/src/pages/test/api-test.vue`
- `frontend/src/apis/DOCTOR_API.md`
- `frontend/src/apis/index.ts` 的直播、用户、部分活动详情方法

当前判断：

- 首页、咨询师列表、搜索、活动列表、文章详情、咨询师详情已切到新 FastAPI。
- 旧引用主要是兼容常量、测试页或暂未启用功能。

建议：

- 不建议马上全删，先完成动态接口联调。
- 上线前按页面访问链路再清理未使用旧接口。

## 后端侧疑似问题

### 8. `database.py` 开启了 SQL echo

当前：

```python
echo=True
```

影响：

- 调试时有帮助。
- 联调/生产会输出大量 SQL 日志，可能泄露参数或影响日志可读性。

建议：

- 本地排查暂时保留。
- 稳定后改成由环境变量控制，生产关闭。

### 9. `config.py` 存在明文默认密码

当前默认值：

```text
DB_PASSWORD=Aa1!StrongPwd2026
```

影响：

- 如果代码进入共享仓库或部署环境，会有凭据泄露风险。

建议：

- 改为从 `.env` 读取。
- 代码默认值留空或使用占位符。

### 10. `.env.example` 不会自动生效

当前只有：

```text
backend-python/.env.example
```

该文件只是安全模板；`pydantic-settings` 默认读取环境变量和复制后的 `.env`，
不会直接读取 `.env.example`。

影响：

- 你以为 `.env.example` 的配置会自动生效，但实际运行仍使用环境变量、`.env`
  或 `config.py` 默认值。

建议：

- 开发期直接设置系统环境变量，或复制为 `.env` 并在 `Settings` 里配置 `env_file`。
- 最简单排查方式：每次启动前运行“打印后端数据库配置摘要”的命令确认实际生效值。

## 建议修复顺序

1. 先统一数据库配置：确定使用 `lxxlBuild` 还是其他库，统一 `config.py`、建表脚本、SQL Server 实际数据库。
2. 确认认证方式：本地优先用 `.\SQLEXPRESS + Trusted_Connection=true`；生产再使用用户名密码。
3. 重新执行建表脚本，确保表创建在 FastAPI 正在连接的库里。
4. 执行 `seed_demo_data.py`，确认输出 `[OK] demo data seeded`。
5. 访问 `/api/mini/common/banners`、`/api/mini/common/counselors`、`/api/mini/ops/activities`，确认都是 200 且有数据。
6. 刷新 H5 首页，检查控制台是否还出现 fallback 到 mock 的提示。
7. 检查图片 Network 请求域名，如图片 404，再修 `frontend/src/utils/image.ts`。

## 推荐验证命令

后端实际配置：

```powershell
cd d:\data\extra\lxxl-main
python -c "import sys; sys.path.insert(0, r'backend-python'); from config import settings; print('DB_SERVER=', settings.DB_SERVER); print('DB_PORT=', settings.DB_PORT); print('DB_NAME=', settings.DB_NAME); print('DB_USER=', settings.DB_USER); print('DB_TRUSTED_CONNECTION=', settings.DB_TRUSTED_CONNECTION); print('DB_DRIVER=', settings.DB_DRIVER)"
```

执行 seed：

```powershell
cd d:\data\extra\lxxl-main
& "backend-python\venv\Scripts\python.exe" "backend-python\seed_demo_data.py"
```

接口冒烟：

```powershell
python -c "import requests; base='http://127.0.0.1:8000'; paths=['/openapi.json','/api/mini/common/banners','/api/mini/common/counselors','/api/mini/ops/activities'];\nfor p in paths:\n    r=requests.get(base+p, timeout=5)\n    print(p, r.status_code, r.text[:300])"
```

前端静态检查：

```powershell
cd d:\data\extra\lxxl-main\frontend
pnpm type-check
```

## 当前已通过检查

- FastAPI 服务可启动。
- `/openapi.json` 返回 200。
- 本次编辑的前端文件 lint 无报错。
- `backend-python/common.py` 和 `backend-python/seed_demo_data.py` 语法检查通过。
