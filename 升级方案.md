# lxxl 统一微信小程序升级方案

版本：v1  
日期：2026-03-28

## 1. 方案目标

本方案的目标不是简单“把几个页面搬进微信小程序”，而是把当前分散在官网、微信 H5、测试微信站、专家版、uni-app、新旧后台工作台中的能力，统一为一套以微信小程序为主入口的业务系统。

本次升级要解决的核心问题：

1. 多入口并存，用户、咨询师、助理、运营、管理员分别走不同入口，认知和维护成本都高。
2. 前端实现割裂，既有 Razor 页面，也有微信 H5 页面，还有新的 uni-app 前端。
3. 认证体系不统一，既有 `Session`，也有轻量 token，微信登录链路也分散。
4. 预约、支付、咨询记录、后台履约没有统一的角色工作台视图。
5. 内容、Banner、活动、表单等能力虽然在同一库里，但交付方式不统一。

## 2. 当前系统现状

### 2.1 当前渠道形态

当前系统大致可以拆成 6 套形态：

| 形态 | 说明 |
| --- | --- |
| 官网公开站 | 内容门户、文章、知识、咨询师展示 |
| 微信正式站 | 当前核心微信 H5 预约链路 |
| 微信测试站 | 正式站的测试/副本版本 |
| 专家版微信站 | 专家版咨询入口 |
| 新 uni-app 前端 | 已有首页、登录、咨询师、个人中心等新页面 |
| 后台/工作台 | 运营后台、咨询师工作台、患者中心 |

### 2.2 当前角色体系

当前代码中明确存在以下角色：

| 角色 | 当前登录入口 | 当前主要落页 |
| --- | --- | --- |
| 游客 | 无需登录 | 官网、微信站、新前端首页 |
| 患者 | 微信登录、新前端登录 | `/we/Index`、`/Patient/UserCenter` |
| 咨询师 | `/psyLogin/Index`、微信登录 `logintype=1` | `/psychologists/Index` |
| 超级管理员 | `/AdminLogin/Index` | `/main/Index` |
| 咨询助理 | `/AdminLogin/Index` | `/main/IndexAssistant` |
| 运营 | `/AdminLogin/Index` | `/main/IndexSCYY` |

### 2.3 当前架构问题

1. 角色和渠道高度耦合。
2. 微信用户链路主要依赖 H5 和 `Session`。
3. 小程序前端基础已经有了，但没有成为唯一主前端。
4. 后台能力集中在 `mainController`，职责过重，接口边界不清晰。
5. 预约支付仍以 H5 JSAPI 页面跳转为主，不是原生小程序支付链路。

## 3. 升级原则

### 3.1 总原则

1. 一个统一业务入口：所有对内对外业务入口统一到一个微信小程序。
2. 一个统一前端基座：以现有 `frontend/` 的 uni-app 项目作为唯一前端基座，目标平台聚焦 `mp-weixin`。
3. 一个统一认证中心：角色不再由入口决定，而由登录后的身份和角色绑定决定。
4. 一个统一 API 体系：面向小程序重组 API，而不是继续直接暴露历史 MVC 页面。
5. 一个统一支付链路：全部改成小程序原生支付，不再依赖 H5 支付页跳转。

### 3.2 现实原则

“整套系统统一成微信小程序”在产品视角上是成立的，但在工程实现上要区分：

1. 用户、患者、咨询师、助理、运营轻工作台，适合进入小程序。
2. 重型后台能力，比如复杂表格、批量导入导出、富文本维护、组织权限配置，短期不适合直接全量小程序化。

因此，本方案给出两个口径：

\begin{longtable}{@{}p{0.18\linewidth}p{0.76\linewidth}@{}}
\toprule
口径 & 说明\tabularnewline
\midrule
\endhead
产品口径 & 整个业务系统统一成一个微信小程序\tabularnewline
工程口径 & 一期统一前台和角色工作台到一个小程序，重后台保留 Web 控制台过渡，二期再决定是否继续把轻后台迁入小程序\tabularnewline
\bottomrule
\end{longtable}

本方案的推荐路线是：

**一个主小程序 + 一套重构后的后端 API + 一个保留过渡期的后台 Web 控制台**

## 4. 推荐目标架构

### 4.1 目标结构

```text
微信小程序（唯一业务入口）
  ├─ 游客端
  ├─ 患者端
  ├─ 咨询师工作台
  ├─ 咨询助理工作台
  ├─ 运营轻工作台
  └─ 管理员轻控制台（可选）

统一后端 API
  ├─ 认证与账号中心
  ├─ 内容中心
  ├─ 咨询与预约中心
  ├─ 排班中心
  ├─ 支付中心
  ├─ 表单与活动中心
  ├─ 工作台中心
  └─ 审计与消息中心

兼容层
  ├─ 旧 MVC 页面逐步下线
  ├─ 旧 Session 登录逐步替换
  └─ 旧后台管理暂时保留
```

### 4.2 前端技术路线

直接基于现有 `frontend/` 继续演进，不新起一套框架。

原因：

1. 现有前端本身就是 uni-app，天然支持 `mp-weixin`。
2. 已经有首页、咨询师列表、咨询师详情、登录、个人中心等基础页面。
3. 统一到小程序后，只需要把目标平台从“多端尝试”收敛成“优先微信小程序”。

建议前端策略：

1. 保留 uni-app。
2. 以微信小程序为主构建目标。
3. 页面按角色和业务拆成分包。
4. 停止继续维护新的 Razor 页面和新的 H5 业务页。

## 5. 小程序信息架构

### 5.1 推荐分包设计

| 分包 | 目标角色 | 核心页面 |
| --- | --- | --- |
| 主包 `main` | 游客、患者 | 首页、登录、咨询师列表、咨询师详情、搜索、我的 |
| 分包 `consultation` | 患者 | 预约创建、协议签署、支付、订单、咨询记录、通知 |
| 分包 `patient` | 患者 | 个人资料、登记表、我的咨询、我的订单、消息中心 |
| 分包 `counselor` | 咨询师 | 工作台首页、今日待服务、排班、咨询单、咨询记录、个案记录 |
| 分包 `assistant` | 咨询助理 | 咨询列表、待处理任务、预约协调、风险提醒、消息跟进 |
| 分包 `ops` | 运营、管理员 | Banner、活动、主题月、内容发布、基础统计、审核任务 |
| 分包 `admin-webview` | 管理员 | 仅过渡期使用，嵌入重后台能力或跳转外部后台 |

### 5.2 底部 Tab 设计

建议统一成 5 个一级入口：

1. 首页
2. 咨询
3. 活动
4. 工作台
5. 我的

说明：

1. 游客和患者看到的是标准 Tab。
2. 咨询师、助理、运营登录后，`工作台` Tab 按角色动态展示不同首页。
3. 管理员默认进入工作台，但复杂后台仍可通过工作台进入外部管理页。

### 5.3 页面映射

| 目标小程序模块 | 处理方式 |
| --- | --- |
| 主包首页、内容页、知识页 | 全量迁入小程序 |
| 主包 + `consultation` 分包 | 作为主要迁移来源 |
| 不再单独保留 | 合并到正式小程序环境开关 |
| 咨询师详情和专家服务页 | 合并到咨询师模块 |
| 继续保留并增强 | 作为新主前端 |
| `patient` 分包 | 患者中心迁入小程序 |
| `counselor` 分包 | 咨询师工作台迁入小程序 |
| `counselor` 分包 | 合并到咨询师工作台 |
| `ops` 分包 + `admin-webview` | 轻量功能迁入，小程序不适合的保留 Web |
| `patient` 分包 + `ops` 分包 | 表单设计和活动发布分角色处理 |
| 原生支付能力 | 页面下线，改成 API + `wx.requestPayment` |

## 6. 角色设计

### 6.1 目标角色模型

建议统一成“账号 + 角色绑定”模型，而不是继续把入口、角色、数据表绑死。

目标角色：

| 角色 | 说明 |
| --- | --- |
| Visitor | 未登录用户 |
| Patient | 患者 |
| Counselor | 咨询师 |
| Assistant | 咨询助理 |
| Ops | 运营 |
| Admin | 管理员 |

### 6.2 当前表结构与目标关系

当前身份数据分散在：

1. `T_User`
2. `T_Doctor`
3. `T_Admin`

还要特别注意一个历史兼容问题：

1. `T_User.OpenID` 和 `T_Doctor.openid` 多半承接的是公众号/H5 时代的身份标识。
2. 微信小程序 `openid` 与公众号 `openid` 不是同一个标识，不能直接当成同一个账号主键。
3. 如果现网能拿到 `unionid`，优先用 `unionid` 做身份归并。
4. 如果现网没有完整 `unionid`，一期必须依赖手机号绑定完成账号合并。

建议不要一开始就硬做大一统迁移，而是分两步：

| 阶段 | 策略 |
| --- | --- |
| 一期 | 新增统一账号表与映射表，保留 `T_User`、`T_Doctor`、`T_Admin` 作为业务主表 |
| 二期 | 再逐步收敛用户主档、手机号、OpenID、UnionID 和角色绑定 |

建议新增表：

1. `AppAccount`
2. `AppRoleBinding`
3. `AppLoginSession`
4. `AppRoleSwitchLog`

建议字段重点：

1. `unionid`
2. `openid`
3. `mobile`
4. `isActive`
5. `defaultRole`

## 7. 后端改造方案

### 7.1 认证改造

当前问题：

1. 旧系统依赖 `Session`。
2. 新系统 token 很轻，不能支撑完整角色体系。
3. 微信 H5 登录链路基于网页 OAuth，不适合小程序主链路。

目标方案：

1. 小程序前端通过 `wx.login` 获取 `code`。
2. 后端通过 `code2Session` 换取 `openid` / `session_key`。
3. 建立统一账号与角色绑定关系。
4. 返回自己的业务 token。
5. 所有小程序 API 使用统一 token 鉴权。

关键约束：

1. 不要直接沿用旧公众号 `openid` 作为小程序身份主键。
2. 一期必须显式区分 `mp_openid`、`oa_openid`、`unionid`、`mobile`。
3. 身份兼容要通过账号映射层完成，而不是直接在旧业务表上硬改字段。

建议新接口：

| 接口 | 作用 |
| --- | --- |
| `POST /api/mini/auth/login` | 小程序登录，返回 token、角色列表、默认角色 |
| `POST /api/mini/auth/bind-mobile` | 绑定手机号 |
| `POST /api/mini/auth/switch-role` | 角色切换 |
| `GET /api/mini/auth/me` | 当前账号信息 |

### 7.2 API 重组

当前 `frontendController`、`loginController`、`userCenterController` 只能覆盖一小部分能力，不足以承接“整套系统小程序化”。

建议按业务域重组 API：

| API 域 | 作用 |
| --- | --- |
| `/api/mini/auth/*` | 认证、角色、账号 |
| `/api/mini/common/*` | 首页、Banner、内容、搜索、咨询师公开信息 |
| `/api/mini/patient/*` | 患者资料、订单、咨询、表单、消息 |
| `/api/mini/counselor/*` | 咨询师排班、咨询单、记录、个案、统计 |
| `/api/mini/assistant/*` | 待处理咨询、协同任务、消息、跟进 |
| `/api/mini/ops/*` | Banner、活动、主题月、文章、轻量审核 |
| `/api/mini/admin/*` | 管理员控制接口，短期只放轻量能力 |
| `/api/payment/wechat/*` | 小程序支付、回调、对账 |

### 7.3 支付改造

当前支付链路是：

1. 下单
2. 跳转 `TenPayV3/PublicPay`
3. 页面中调起 JSAPI 支付

小程序目标链路应该改成：

1. 小程序调用 `POST /api/payment/wechat/create`
2. 后端统一下单
3. 返回 `timeStamp`、`nonceStr`、`package`、`signType`、`paySign`
4. 前端直接调用 `wx.requestPayment`
5. 支付结果由后端异步通知更新订单

实现边界：

1. 商户号、签名、回调、订单状态更新等后端能力可以尽量复用现有 `TenPayV3Controller` 和 `WxService` 中的支付资产。
2. 但小程序前端不能再复用现有 H5 支付页和跳转逻辑。
3. 旧支付页面只能作为兼容层，不再作为主流程。

需要下线或弱化的旧能力：

1. `Views/TenPayV3/*`
2. H5 支付跳转页
3. 基于 H5 的支付回跳依赖

### 7.4 内容与搜索改造

当前内容散落在：

1. `T_Content`
2. `T_Banner`
3. `T_Menu`
4. 多个 Razor 列表页

目标方案：

1. 内容 API 统一返回小程序可渲染结构。
2. 图片路径统一绝对 URL。
3. 富文本统一做清洗和组件适配。
4. 官网内容、知识、主题月、Banner、活动都通过统一内容中心下发。

### 7.5 文件与媒体改造

当前很多上传逻辑基于本地路径和 MVC 表单提交，不适合小程序长期使用。

建议：

1. 小程序统一走 `wx.uploadFile`。
2. 服务端增加统一上传接口。
3. 文件落对象存储或统一媒体目录。
4. 返回稳定的媒体 URL 和缩略图 URL。

### 7.6 消息与通知改造

建议把当前消息体系拆成两类：

1. 站内消息：小程序消息中心。
2. 微信订阅消息：预约成功、改期提醒、支付成功、咨询提醒、待处理提醒。

新增建议：

1. 预约提醒任务表
2. 订阅消息模板映射表
3. 消息发送日志表

## 8. 小程序模块设计

### 8.1 游客/患者模块

需要覆盖的能力：

1. 首页
2. 搜索
3. 内容与知识
4. 咨询师列表
5. 咨询师详情
6. 预约
7. 协议
8. 支付
9. 我的咨询
10. 我的订单
11. 我的登记表
12. 我的通知

### 8.2 咨询师模块

建议把当前 `psychologistsController` 和 `doctorController` 的能力统一成一个咨询师工作台。

核心页面：

1. 工作台首页
2. 今日咨询
3. 待确认咨询
4. 排班管理
5. 咨询记录
6. 个案记录
7. 个人资料
8. 统计看板

### 8.3 咨询助理模块

从当前 `IndexAssistant` 和部分后台协同能力中抽出。

核心页面：

1. 待处理咨询
2. 高风险提醒
3. 排期协调
4. 消息跟进
5. 患者联系记录

### 8.4 运营模块

从当前 `IndexSCYY` 和 `mainController` 中抽运营轻工作台能力。

核心页面：

1. Banner 管理
2. 活动管理
3. 主题月管理
4. 内容发布
5. 数据看板

### 8.5 管理员模块

管理员能力分成两层：

| 层级 | 说明 |
| --- | --- |
| 小程序轻控制台 | 审核、查看、简单修改、数据确认 |
| Web 重后台 | 复杂配置、批量维护、导入导出、组织权限 |

推荐做法：

1. 一期不追求把所有 `main/*` 都搬进小程序。
2. 小程序先承接高频、轻量、碎片化操作。
3. 复杂后台继续保留 Web。

## 9. 迁移路径

### 9.1 阶段划分

#### 阶段 0：现状冻结与统一边界

目标：

1. 停止新增新的 Razor 业务页。
2. 停止扩散新的 H5 业务入口。
3. 明确哪些功能进小程序，哪些先保留后台 Web。

交付物：

1. 角色清单
2. 页面迁移清单
3. API 清单
4. 数据映射清单

#### 阶段 1：底座改造

目标：

1. 建统一认证中心
2. 建统一 token 鉴权
3. 建统一小程序 API 网关
4. 打通小程序支付
5. 建统一上传与消息机制

交付物：

1. `/api/mini/auth/*`
2. `/api/payment/wechat/*`
3. 账号映射表
4. token 中间件

#### 阶段 2：患者侧小程序上线

目标：

1. 首页
2. 内容
3. 咨询师列表和详情
4. 登录
5. 预约
6. 支付
7. 患者个人中心

阶段完成后可以下线：

1. `Views/we/*` 的大部分用户页
2. `Views/weTest/*`
3. 部分 `Views/Patient/*`

#### 阶段 3：咨询师与助理工作台上线

目标：

1. 咨询师工作台
2. 排班与咨询记录
3. 助理待办
4. 风险与协同任务

阶段完成后可以下线：

1. `Views/psychologists/*` 的高频操作页
2. `Views/doctor/*`
3. 部分助理后台页

#### 阶段 4：运营与管理员轻工作台上线

目标：

1. Banner、活动、主题月、内容的运营轻量操作进入小程序
2. 审核和查看型后台能力迁入小程序
3. 重后台保留 Web

阶段完成后：

1. 管理端形成“小程序轻后台 + Web 重后台”的稳定结构
2. 再决定是否继续把重后台进一步小程序化

### 9.2 里程碑建议

| 阶段 | 周期建议 | 结果 |
| --- | --- | --- |
| 阶段 0 | 1 周 | 完成范围确认 |
| 阶段 1 | 2-3 周 | 完成认证、支付、API 底座 |
| 阶段 2 | 3-5 周 | 患者端小程序上线 |
| 阶段 3 | 3-5 周 | 咨询师/助理工作台上线 |
| 阶段 4 | 2-4 周 | 运营轻工作台与轻后台上线 |

总周期建议：**11-18 周**

## 10. 推荐实施顺序

### 10.1 先做什么

必须优先做的 5 件事：

1. 统一认证
2. 统一支付
3. 统一咨询师详情与预约 API
4. 统一患者中心 API
5. 统一图片与富文本输出格式

### 10.2 后做什么

第二批做的能力：

1. 咨询师工作台
2. 助理协同
3. 运营轻后台
4. 表单和活动全量迁移

### 10.3 暂缓什么

建议暂缓直接全量搬迁的能力：

1. 复杂后台列表页
2. 批量导入导出
3. 富文本重编辑器
4. 特别重的统计报表

## 11. 风险与应对

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| 角色体系过于分散 | 患者、咨询师、管理员当前分表分登录 | 先做账号映射层，不直接大迁表 |
| 小程序不适合重后台 | 复杂后台体验差 | 一期保留 Web 重后台 |
| 旧支付链路依赖 H5 页面 | 不能直接复用 | 直接改成小程序支付 |
| 富文本与图片兼容性 | 旧内容可能不适配小程序 | 统一内容渲染层 和图片处理层 |
| 旧 Session 逻辑太多 | 历史控制器耦合高 | 新 API 不再依赖 Session，旧系统逐步退出 |
| 控制器过重 | `mainController` 边界混乱 | 以新 API 域拆分，先不重写旧页面 |

## 12. 验收标准

### 12.1 一期验收

满足以下条件即认为“小程序统一化”正式成立：

1. 患者从小程序可以完成登录、浏览、预约、支付、查看订单、查看咨询记录。
2. 咨询师可以从同一个小程序进入自己的工作台并维护排班和咨询记录。
3. 助理和运营可以从同一个小程序进入自己的任务页。
4. 所有用户登录都从同一个小程序入口开始，而不是不同站点入口。
5. 微信 H5 正式预约链路不再是主链路。

### 12.2 二期验收

满足以下条件即认为“系统基本完成统一”：

1. `we`、`weTest`、`experts` 不再承担主业务入口。
2. 患者、咨询师、助理、运营都以小程序为日常入口。
3. Web 后台只剩重后台管理和重配置能力。

## 13. 结论

这次升级不建议理解成“把当前所有页面原样改成小程序页面”，而应该理解成：

**把当前 6 套入口、6 类角色、若干割裂的前端实现，收敛成一个微信小程序入口、一个统一账号体系、一套统一 API、一条统一支付链路。**

推荐的最终状态是：

1. **一个微信小程序承接所有用户与角色入口**
2. **现有 `frontend/` 成为唯一主前端**
3. **后端围绕小程序重新组织 API**
4. **患者、咨询师、助理、运营进入同一个小程序，不同角色看到不同工作台**
5. **重后台先保留 Web，作为过渡与兜底**
