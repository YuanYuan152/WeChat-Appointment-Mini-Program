# 升级方案符合度审计

对照《升级方案.md》各章节，逐项核查当前代码与业务实现，给出 **已对齐 / 部分对齐 / 缺失** 三个状态以及差距说明。本文档同步记录本轮已修复项。

> 审计结论速读：核心底座（§6 角色 / §7.1 认证 / §7.2 API / §7.3 支付 / §7.5 上传 / §7.6 消息）已全部落地。一期患者-咨询师-助理-运营工作台均已上线。剩余偏差集中在「冗余/演示页未接 API」「分包路由结构」「订阅消息触发点」「unionid 归并」。

---

## 1. §6 角色与账号体系

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 6 类角色定义（Visitor/Patient/Counselor/Assistant/Ops/Admin） | 已对齐 | `models.py::AppRoleBinding.RoleType` 枚举一致 |
| `AppAccount` | 已对齐 | 含 `OpenId / UnionId / Mobile / Nickname / AvatarUrl / ActiveRole / RealName / Gender / Birthday / EmergencyContact / EmergencyPhone` |
| `AppRoleBinding` | 已对齐 | 一账号多角色 |
| `AppLoginSession` | 已对齐 | token + session_key 持久化 |
| `AppRoleSwitchLog` | 已对齐 | `auth.py::switch-role` 写日志 |
| 与旧表 `T_User / T_Doctor / T_Admin` 的映射层 | 部分对齐 | `common_counselor_detail` 已有 T_Doctor 回退；`AppAccount` 与 `T_User` 之间尚未建立 OpenId 自动迁移脚本，依赖手动绑定 |
| `unionid` 归并 | **缺失** | `auth.login` 仅取 openid，没有读取 `unionid`；多端账户合并暂未实现 |

---

## 2. §7.1 统一认证（4 接口）

| 接口 | 状态 |
| --- | --- |
| `POST /api/mini/auth/login` | 已对齐（`auth.py::login`） |
| `POST /api/mini/auth/bind-mobile` | 已对齐（`auth.py::bind_mobile`） |
| `POST /api/mini/auth/switch-role` | 已对齐（含切换日志、Patient 兜底） |
| `GET /api/mini/auth/me` | 已对齐 |

前端 `pages/auth/login.vue` 本轮已修复：
- 给 `<button open-type="getPhoneNumber">` 增加 `@getphonenumber` 回调，登录后自动调用 `bind-mobile`，绑定失败不阻断登录。

剩余建议（可选）：
- `auth.login` 加上 `session_info.get("unionid")`，配合 `AppAccount.UnionId` 做账号归并。
- 在 `bind-mobile` 命中"已被其它账号绑定"时，提供「合并账号」流程而非直接 409。

---

## 3. §7.2 API 重组（8 个域）

| API 域 | 状态 | 关键路由 |
| --- | --- | --- |
| `/api/mini/auth/*` | 已对齐 | login/bind-mobile/me/switch-role |
| `/api/mini/common/*` | 已对齐 | banners/articles/counselors/search |
| `/api/mini/patient/*` | 已对齐 | orders/me/registration/**consultations**（本轮新增） |
| `/api/mini/counselor/*` | 已对齐 | schedules/consultations/case-records/profile/stats |
| `/api/mini/assistant/*` | 已对齐 | tasks/risk-alerts/schedule-overview/contact-records |
| `/api/mini/ops/*` | 已对齐 | banners/activities/articles/dashboard/users |
| `/api/mini/admin/*` | 部分对齐 | 仅有 users + bind/unbind role；缺审计型轻能力 |
| `/api/payment/wechat/*` | 已对齐 | create + callback（mock + 正式两条链路） |

附加：
- `/api/mini/message/*` 站内消息中心已实现（list/unread-count/markRead/subscribe/system/remind-tasks）。

---

## 4. §7.3 支付改造

| 项 | 状态 |
| --- | --- |
| `POST /api/payment/wechat/create` 返回 wx.requestPayment 五参数 | 已对齐 |
| `POST /api/payment/wechat/callback` 异步通知 + `AppOrder.Status=PAID` | 已对齐 |
| 真实模式（配置 `WECHAT_PAY_*`）走统一下单 + MD5 签名 | 已对齐 |
| Mock 模式回退（本地联调） | 已对齐 |
| 前端不再走 H5 跳转、改 `wx.requestPayment` | 已对齐（`consultant/detail.vue` 已用 `uni.requestPayment`） |

剩余建议：
- 支付成功页 / 订单详情页应主动调起 `wx.requestSubscribeMessage`，触发"预约成功 / 支付成功"订阅消息授权。当前 `pages/patient/orders/list.vue` 还没接订阅。

---

## 5. §7.4 内容与搜索

| 项 | 状态 |
| --- | --- |
| 内容统一返回 JSON 结构（不再 Razor） | 已对齐 |
| 图片绝对 URL | 已对齐（本轮修复） |
| 搜索（counselor/article/activity 三库） | 已对齐（`common.py::common_search`） |

本轮修复：
- `frontend/src/utils/image.ts` 重写为「智能路由」：
  - `/static/uploads/*`、`/api/static/*` → `API_V2_CONFIG.baseURL`（FastAPI）
  - 其它 `/static/*` → 直接返回（小程序本地资源）
  - 其它 `/...` → 兼容旧 C# 后端
  - 之前所有相对路径都拼旧域名导致演示数据图片 404 已解决。

---

## 6. §7.5 文件与媒体

| 项 | 状态 |
| --- | --- |
| `POST /api/upload/file` 对接 wx.uploadFile | 已对齐 |
| 鉴权 + 大小限制（10MB） + 类型白名单 | 已对齐（仅 image/*） |
| 落本地静态目录 + 返回绝对 URL | 已对齐 |

剩余建议：
- 后续接登记表附件、咨询记录附件时，扩展 `ALLOWED_TYPES` 至 PDF / 音频。
- 生产建议替换为 OSS/COS（`upload.py` 已为此预留扩展点）。

---

## 7. §7.6 消息与通知

| 项 | 状态 |
| --- | --- |
| 站内消息：`AppMessage` 表 + list/unread-count/markRead | 已对齐 |
| 微信订阅消息：发送日志 `AppMessageLog` + 模板映射 `AppSubscribeTemplate` | 已对齐（model + subscribe 接口已就位） |
| 提醒任务：`AppRemindTask` 表 + create/process | 已对齐 |
| **前端订阅消息授权调起** | **缺失** | 还没在「预约成功 / 支付成功 / 订单详情」页主动调 `wx.requestSubscribeMessage` 拿模板 ID |

---

## 8. §5.1 分包 & §5.2 TabBar

### TabBar（5 个一级入口）

| 方案要求 | 当前 `pages.json` |
| --- | --- |
| 首页 / 咨询 / 活动 / 工作台 / 我的 | 首页 / 活动 / 咨询 / 工作台 / 我的 |

✅ 5 个齐全，仅顺序不同（不影响功能）。

### 分包

| 方案分包 | 当前实现 | 状态 |
| --- | --- | --- |
| `main` 主包 | 主包页面齐全 | 已对齐 |
| `consultation` 分包（预约动线：预约引导 / 协议） | 已对齐（本轮 P2） | `pages/consultation/booking/create`、`pages/consultation/agreement/{xy1,xy2}` |
| `patient` 分包（个人中心：订单/记录/登记表/消息/资料） | 已对齐 | orders/records/registration/profile/messages |
| `counselor` 分包 | 已对齐 | workbench/schedule/consultations/case-record/profile/stats |
| `assistant` 分包 | 已对齐 | workbench/tasks/risk/contact |
| `ops` 分包 | 已对齐 | index（运营工作台）/banner/articles/activities/themes/dashboard/admin/admin-roles |
| `admin-webview` 分包 | 已对齐（本轮 P2） | 独立分包 `pages/admin-webview/index`，含确认页 + webview 容器 |

> 备注：方案 §5.1 表格中将"我的订单 / 我的咨询 / 消息"同时列在 `consultation` 与 `patient` 两个分包，本实现按"业务动线 vs 个人中心"区分——预约动线归 `consultation`，回看类归 `patient`，避免功能重复定义。

---

## 9. §8 各角色模块完成度

### 9.1 §8.1 患者

| 方案要求 | 状态 | 备注 |
| --- | --- | --- |
| 首页 | 已对齐 | `pages/index/index.vue` 走 V2 |
| 搜索 | 已对齐 | `pages/search/index.vue` |
| 内容/知识 | 已对齐 | 主页 `articles` + `pages/article/detail.vue` |
| 咨询师列表 | 已对齐 | `pages/consultant/list.vue` |
| 咨询师详情 | 已对齐 | `pages/consultant/detail.vue` 含真实 schedule + 支付 |
| 预约 | **部分对齐** | 真实预约链路在 `consultant/detail`；冗余页 `pages/appointment/create.vue` 仍是硬编码假数据，**建议保留为入口指引或下线** |
| 协议 | 已对齐 | `pages/xy/xy1.vue`/`xy2.vue` 静态文本即可 |
| 支付 | 已对齐 | `wx.requestPayment` |
| 我的咨询 | 已对齐 | 本轮新增：`patient/records/list.vue` 接 `/api/mini/patient/consultations` |
| 我的订单 | 已对齐 | 本轮修复：删除重复模板、接 V2 `/api/mini/patient/orders` |
| 我的登记表 | 已对齐 | `patient/registration/edit.vue` |
| 我的通知 | 已对齐 | `patient/messages/index.vue` |

### 9.2 §8.2 咨询师工作台（8 项）

| 工作台首页 / 今日咨询 / 待确认 / 排班 / 咨询记录 / 个案 / 个人资料 / 统计 |
| --- |
| 全部已对齐（`pages/counselor/*`），数据均走 `/api/mini/counselor/*` |

### 9.3 §8.3 助理工作台（5 项）

| 待处理咨询 / 高风险提醒 / 排期协调 / 消息跟进 / 患者联系记录 |
| --- |
| 全部已对齐（`pages/assistant/*` + `/api/mini/assistant/*`） |

### 9.4 §8.4 运营工作台（5 项）

| 项 | 状态 |
| --- | --- |
| Banner 管理 | 已对齐 |
| 活动管理 | 已对齐 |
| 主题月管理 | 已对齐（本轮 P2） | 前台 `pages/theme/index.vue` 接 `ops.activities?type=THEME`；运营端独立 `pages/ops/themes/index.vue` CRUD 页（含状态色标、启停切换、编辑回填） |
| 内容发布 | 已对齐（`ops/articles`） |
| 数据看板 | 已对齐（`ops/dashboard`） |

### 9.5 §8.5 管理员

| 项 | 状态 |
| --- | --- |
| 小程序轻控制台（审核 / 查看 / 简单修改 / 数据确认） | 已对齐（`ops/admin` + `ops/admin-roles`） |
| Web 重后台保留作过渡 | 已对齐（旧 .NET MVC 后台保留） |
| 小程序里跳外部后台 webview | 已对齐（本轮 P2） | 独立分包 `pages/admin-webview/index`，含目标地址确认页 + webview 嵌入；由 `VITE_ADMIN_WEB_URL` 配置 |

---

## 10. §12.1 一期验收清单

| 验收项 | 是否闭环 |
| --- | --- |
| 患者从小程序完成 登录→浏览→预约→支付→订单→咨询记录 | ✅（本轮把订单页 / 咨询记录页修通） |
| 咨询师从同一小程序进入工作台维护排班、咨询记录 | ✅ |
| 助理 / 运营从同一小程序进入任务页 | ✅ |
| 所有用户登录都从同一小程序入口开始 | ✅ |
| 微信 H5 正式预约链路不再是主链路 | ✅（`VITE_USE_BOOKING_MOCK=false` 后所有列表 / 详情 / 预约都走 V2） |

一期验收 5 条已 **全部具备闭环条件**，仅剩订阅消息授权 UI 是体验性补强。

---

## 11. 本轮已修复（一次性提交建议）

### 11.1 第一批 — 业务闭环修复

| # | 文件 | 类型 | 解决了什么 |
| --- | --- | --- | --- |
| 1 | `frontend/src/utils/image.ts` | 修复 | `/static/uploads/*` 走 V2，前端本地 `/static/*` 直通；解决 seed 图片 404 |
| 2 | `frontend/src/pages/patient/orders/list.vue` | 修复 | 删除重复模板/脚本块，避免 Vue 编译歧义；订单接 `patient.orders` |
| 3 | `frontend/src/pages/patient/records/list.vue` | 实现 | 替换骨架页，接新增的 `patient.consultations` |
| 4 | `frontend/src/pages/auth/login.vue` | 实现 | 改为 `getphonenumber` 事件回调，登录后自动 `bindMobile` |
| 5 | `frontend/src/pages/theme/index.vue` | 实现 | 替换硬编码假数据，接 `ops.activities?type=THEME`，含上拉刷新 |
| 6 | `backend-python/patient.py` | 新增 | `GET /api/mini/patient/consultations`（关联 `AppCounselorProfile` 输出咨询师姓名/头像） |
| 7 | `frontend/src/config/api.ts` | 新增 | `patient.consultations` 端点常量 |

### 11.2 第二批 — P1 收尾（升级方案剩余偏差）

| # | 文件 | 类型 | 解决了什么 |
| --- | --- | --- | --- |
| 8 | `backend-python/models.py` | 修复 | 用户可见文本列从 `String` 改为 `Unicode / UnicodeText`，从源头解决 SQL Server 中文乱码 |
| 9 | `backend-python/migrate_to_nvarchar.py` | 新增 | 一次性把已存在的 `VARCHAR` 列升级为 `NVARCHAR`（共 52 列），可重复执行 |
| 10 | `backend-python/seed_demo_data.py` | 升级 | 全量改为中文 seed；增加 cleanup_legacy_demo 清理旧 `???` 数据；增加订阅模板 seed |
| 11 | `backend-python/auth.py::login` | 增强 | 提取 `session_info.unionid` 写入 `AppAccount.UnionId`；新登录走 unionid → openid 双层归并 |
| 12 | `backend-python/message.py` | 新增 | `GET /api/mini/message/templates` 暴露 active 订阅模板 ID 列表给前端 |
| 13 | `frontend/src/pages/consultant/detail.vue` | 增强 | 下单成功后调起 `wx.requestSubscribeMessage`，授权结果回写 `/message/subscribe`，再调起支付（升级方案 §7.6） |
| 14 | `frontend/src/pages/appointment/create.vue` | 重写 | 删除写死张医生的演示数据，改为引导页指向咨询师详情，避免误用 |
| 15 | `frontend/src/config/api.ts` | 新增 | `message.templates` 端点常量 |

### 11.3 第三批 — P2 二期方向

| # | 文件 | 类型 | 解决了什么 |
| --- | --- | --- | --- |
| 16 | `frontend/src/pages/ops/themes/index.vue` | 新增 | 运营独立"主题月管理"CRUD 页（增/改/启停/删 / 状态色标 / 编辑回填） |
| 17 | `frontend/src/pages/ops/index/index.vue` | 新增 | 运营工作台首页：网格陈列 7 大入口（含主题月、admin-webview） |
| 18 | `frontend/src/pages/workbench/index.vue` | 修改 | Ops/Admin 角色路由改至运营工作台首页 |
| 19 | `frontend/src/pages/admin-webview/index.vue` | 新增 | 独立分包：目标 URL 确认页 + webview 容器（升级方案 §5.1） |
| 20 | `frontend/src/pages/legal/agreement.vue` | 新增 | 用户协议页（合规） |
| 21 | `frontend/src/pages/legal/privacy.vue` | 新增 | 隐私政策页（合规） |
| 22 | `frontend/src/pages/user/profile.vue` | 修改 | 加注销账号按钮（双重确认）+ 用户协议/隐私政策菜单；补 `handleLogout / handleAvatarClick / goToSettings / formatPhone` 缺失方法 |
| 23 | `frontend/src/pages/auth/login.vue` | 修改 | 底部协议改为可点击 `《用户协议》《隐私政策》` |
| 24 | `frontend/src/pages/consultation/{agreement/xy1, agreement/xy2, booking/create}.vue` | 新增 | 预约动线下沉到 consultation 分包 |
| 25 | `frontend/src/pages/{xy/xy1, xy/xy2, appointment/create}.vue` | 删除 | 旧主包路径迁移完成后清理 |
| 26 | `frontend/src/pages.json` | 修改 | 注册 consultation / admin-webview 分包；ops 增加 themes/index、index/index；主包加 legal/agreement、legal/privacy；condition 调试入口同步 |
| 27 | `frontend/src/config/api.ts` | 新增 | `auth.deleteAccount` 端点常量 |
| 28 | `frontend/src/apis/auth.ts` | 新增 | `AuthApi.deleteAccount()` |
| 29 | `backend-python/auth.py` | 新增 | `DELETE /api/mini/auth/account` 软注销 + `get_current_account` 校验 IsActive |
| 30 | `backend-python/models.py` | 修改 | `AppAccount` 增加 `IsActive(BIT, default 1) / DeletedAt(DATETIME)` |
| 31 | `backend-python/ensure_schema.py` | 修改 | APP_ACCOUNT_COLUMNS 增加 IsActive / DeletedAt 自动补齐 |

### 11.4 验证结果

- 数据库：`migrate_to_nvarchar.py` 已迁移 52 列，0 失败。
- seed：`AppBanner / AppActivity / AppArticle / AppCounselorProfile / AppSubscribeTemplate` 全部 UTF-8 中文写入成功，旧 `???` 数据已清理。
- 接口体检（UTF-8 解码后）全部 200 且文本正确：
  - `/api/mini/common/banners` → 「专业心理支持 / 家庭关系公益讲座 / 情绪管理主题月」
  - `/api/mini/common/articles` → 「如何判断自己是否需要心理咨询 / 亲子沟通中的三个关键动作 / 情绪急救包」
  - `/api/mini/ops/activities?type=THEME` → 「情绪管理主题月」
  - `/api/mini/message/templates` → 返回 3 个 mock 订阅模板供前端使用

- P2 注销链路体检：
  - `POST /api/mini/auth/login` → 拿 token
  - `DELETE /api/mini/auth/account` → 200 `账号已注销，所有登录会话已失效`
  - 同 token 重新调 `GET /api/mini/auth/me` → 401 Unauthorized（IsActive=False 拦截生效）
  - 数据库验证：`IsActive=False, DeletedAt=<now>, OpenId=deleted_<id>_<ts>`

所有改动 lint 干净。

---

## 12. 仍可优化（按优先级）

### P1 — 一期收尾建议
- [x] `pages/appointment/create.vue` 演示页瘦身为引导页 ✅
- [x] 支付链路调起 `wx.requestSubscribeMessage` + 模板下发 ✅
- [x] `auth.login` 增加 `unionid` 归并 ✅
- [x] `models.py` 文本列改 `Unicode/UnicodeText` + 一次性 NVARCHAR 迁移 ✅

P1 全部完成。

### P2 — 二期方向
- [x] `consultation` 分包：预约 / 协议 类页面按方案 §5.1 下沉到独立分包 ✅
  - `pages/xy/xy1.vue` → `pages/consultation/agreement/xy1.vue`
  - `pages/xy/xy2.vue` → `pages/consultation/agreement/xy2.vue`
  - `pages/appointment/create.vue` → `pages/consultation/booking/create.vue`
  - 订单 / 咨询记录 / 消息留在 `patient` 分包（与方案 §5.1 表格"个人中心"行一致），避免破坏性重构
- [x] `admin-webview` 分包 + 跳转外部 Web 后台的入口页 ✅
  - 新增独立分包 `pages/admin-webview/index.vue`，含确认页 + webview 容器
  - 由 `pages/ops/index/index.vue` 提供入口；`VITE_ADMIN_WEB_URL` 控制目标地址
- [x] 运营独立"主题月管理" UI ✅
  - 新增 `pages/ops/themes/index.vue`：CRUD（增 / 改 / 启停 / 删 / 编辑回填）、状态色标
  - 新增 `pages/ops/index/index.vue` 运营工作台首页，作为 Ops/Admin 的统一落点
  - `pages/workbench/index.vue` 路由表 Ops/Admin 角色指向新工作台首页
- [x] 注销 / 账号删除、隐私政策、用户协议合规页 ✅
  - 新增 `DELETE /api/mini/auth/account` 软注销（清空可识别字段、删 RoleBinding、失效 LoginSession、设 `IsActive=False`、写 `RoleSwitchLog(ToRole=DELETED)`）
  - `models.py::AppAccount` 新增 `IsActive / DeletedAt` 列；`ensure_schema.py` 自动补齐
  - `get_current_account` 增加 IsActive 校验：注销后即使 token 未过期也拒绝（401）
  - 新增 `pages/legal/agreement.vue` 用户协议页、`pages/legal/privacy.vue` 隐私政策页
  - `pages/user/profile.vue` 增加注销按钮（双重确认）+ 用户协议 / 隐私政策菜单入口
  - `pages/auth/login.vue` 底部协议提示改为可点击跳法律页

P2 全部完成。

---

## 13. 验证脚本

### 13.1 数据库 / Seed 重置脚本

```powershell
# 在 backend-python 目录下，按顺序执行：
.\venv\Scripts\python.exe ensure_schema.py          # 1. 建表 + 补 AppAccount 列
.\venv\Scripts\python.exe migrate_to_nvarchar.py    # 2. 把 VARCHAR 升级为 NVARCHAR
.\venv\Scripts\python.exe seed_demo_data.py         # 3. 写入中文演示数据 + 订阅模板
```

### 13.2 接口体检（PowerShell 中务必先 chcp 65001 才能正确显示中文）

```powershell
chcp 65001 | Out-Null
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8

foreach ($p in @(
  "/api/mini/common/banners",
  "/api/mini/common/counselors",
  "/api/mini/common/counselors/1",
  "/api/mini/common/articles",
  "/api/mini/ops/activities",
  "/api/mini/ops/activities?type=THEME",
  "/api/mini/message/templates"
)) {
  $bytes = (Invoke-WebRequest -Uri "http://localhost:8000$p" -UseBasicParsing).RawContentStream.ToArray()
  Write-Host "===== $p ====="
  [System.Text.Encoding]::UTF8.GetString($bytes)
}
```

### 13.3 一期闭环手动验证（建议）

1. 微信开发者工具打开 `frontend/`，确认 `.env` 中 `VITE_USE_BOOKING_MOCK=false`。
2. 首页：banner / 推荐咨询师 / 活动 三个区块都来自 V2。
3. 咨询师详情：选择 schedule → 协议签名 → 下单。
   - 真机会先弹「请求订阅消息」对话框（3 个模板）。
   - 然后调起 `wx.requestPayment`（mock 模式直接成功）。
4. 我的：进「我的订单」「咨询记录」均能拉到刚才生成的记录。
5. 工作台：以 Counselor / Assistant / Ops 角色登录，看 `workbench/index` 路由分发是否正确。
6. 主题月：`/pages/theme/index` 显示后端 `type=THEME` 活动；切换"已结束 / 进行中"标签正常。

### 13.4 升级方案 §12.1 + 合规验收对照（最新）

| 验收点 | 状态 |
| --- | --- |
| 患者从小程序完成 登录→浏览→预约→支付→订单→咨询记录 | ✅ |
| 咨询师 / 助理 / 运营进入同一小程序自己的工作台 | ✅ |
| 所有用户登录都从同一小程序入口开始 | ✅ |
| 微信 H5 正式预约链路不再是主链路 | ✅ |
| 中文内容统一 UTF-8（NVARCHAR）落库 | ✅（P1） |
| 订阅消息授权链路联通 | ✅（P1：mock 模板，配真实 tmplId 即生效） |
| 跨端账号 unionid 归并能力 | ✅（P1：需配置真实 WECHAT_APPID/SECRET 后自动生效） |
| 分包结构按 §5.1 拆分（consultation / admin-webview 独立） | ✅（P2） |
| 运营独立主题月管理 UI | ✅（P2） |
| 注销账号 + 用户协议 + 隐私政策合规页 | ✅（P2） |
