# 测试与生产环境要求

面向当前仓库：`backend-python`（主 API）+ `frontend`（微信小程序）+ 可选 `admin-web` / `EAP_front_site`。数据库为 **SQL Server**，业务主表为 **`App*`**。

---

## 1. 环境隔离（强制）

| 项 | 测试 | 生产 |
|----|------|------|
| 数据库 | 独立库（lxxlBuild_test） | 独立库（lxxlBuild_pro） |
| `.env` / 密钥 | 测试专用 | 生产专用，不可复用开发/测试 JWT、支付密钥 |
| 演示数据 | 可用 `seed_demo_data.py` | **禁止**清表类 seed；默认不灌演示数据 |
| 微信 | 体验版 / 测试 AppID | 正式 AppID，审核发布 |
| 支付 / 短信 | 可 mock | 必须真实；回调 URL 公网 HTTPS |


---

## 2. 公共基础设施

- Python ≥ 3.10，`pip install -r backend-python/requirements.txt`
- SQL Server + **ODBC Driver 17 for SQL Server**
- 服务器能访问数据库；后端对外经 **Nginx（或等价）HTTPS** 反代
- 通用上传和量表图片目录位于发布目录外、服务用户可写且纳入备份；`BASE_URL`
  填对外可访问的 HTTPS 根地址（拼文件绝对路径）
- 避免同一端口多个 uvicorn（旧进程会抢流量）

---

## 3. 数据库（当前设计）

- 主业务：`AppAccount`、`AppOrder`、`AppConsultation`、`AppCounselorProfile`、定价/个案/消息/咨询室等 **`App*` 表**
- 旧表 `T_Doctor` 等为可选兼容；新库可保持 `SKIP_LEGACY_QUERIES=true`
- `ensure_schema.py` 负责普通业务表，但**明确不会创建**以下三张受控量表表：
  `AppAssessmentReport`、`AppAssessmentShareScan`、`AppAssessmentAuditLog`
- **普通业务表建表 / 升级**：

```bash
cd backend-python
# 配好 .env 后
python ensure_schema.py
```

该脚本会按允许自动迁移的模型建缺表，并补增量列。启动时也会跑一轮普通表补齐，发版窗口仍建议主动执行并看日志。

- **EAP 量表受控迁移**：

```bash
# 只读连接目标库，输出服务器、库名和结构差异
python migrate_assessment_tables.py --preflight

# 人工确认输出和目标库，并取得该环境数据库变更批准后才能执行
python migrate_assessment_tables.py --apply --confirm-database lxxlBuild_test
```

生产环境把确认值改成实际生产库名。测试库的确认不能复用于生产库；执行后脚本会校验三张表的
字段、约束和索引。历史 `create_*.py` / `lxxl/App_Data/*.sql` 仅作兼容。

---

## 4. 后端配置要点（`.env`）

从 `backend-python/.env.production` 复制为 `.env` 后按环境填写：

| 变量 | 说明 |
|------|------|
| `DB_SERVER` / `DB_PORT` / `DB_NAME` / `DB_USER` / `DB_PASSWORD` | 指向**本环境**库；服务器常用 `DB_TRUSTED_CONNECTION=false` |
| `JWT_SECRET` | 长随机串，环境间不共用 |
| `WECHAT_APPID` / `WECHAT_SECRET` | 对应小程序账号 |
| `WECHAT_PAY_*` / `WECHAT_PAY_NOTIFY_URL` | 生产必填；回调须 `https://域名/api/payment/wechat/callback` |
| `BASE_URL` | `https://本环境域名` |
| `UPLOAD_DIR` | 通用上传文件持久化目录；必须位于发布目录之外并按环境隔离 |
| `ASSESSMENT_DATA_DIR` | 本环境量表 JSON 的持久化绝对路径；必须位于发布目录之外、环境间隔离并纳入备份 |
| `ASSESSMENT_ASSET_DIR` | 量表封面和结果图片的不可变持久化目录；必须位于发布目录之外、环境间隔离并纳入备份 |
| `ASSESSMENT_SEED_DATA_DIR` | 可选的旧 EAP JSON 初始化目录；初始化完成后通常保持不变 |
| `ASSESSMENT_CONSENT_VERSION` | 当前专业量表隐私同意版本，协议实质变更时提升 |
| `ASSESSMENT_SHARE_SECRET` | EAP 静态分享码独立密钥，至少 32 个字符；测试、生产分别配置且不得复用 `JWT_SECRET` |
| `ASSESSMENT_FRONTEND_BASE_URL` | 扫码后进入的 EAP 站点根地址，如 `https://本环境域名` |
| `SMS_MOCK` | 测试可 `true`；生产 `false` |
| `SKIP_LEGACY_QUERIES` | 无旧库则 `true` |

`ASSESSMENT_SHARE_SECRET` 会决定静态二维码签名；部署后随意更换会使已经传播的旧二维码失效，应纳入环境密钥备份与变更流程。
`ASSESSMENT_CONSENT_VERSION` 必须与 EAP 的 `ASSESSMENT_PRIVACY_VERSION` 同批更新，否则专业量表提交会被拒绝。

`ASSESSMENT_DATA_DIR` 只允许同一环境的后端实例共享。建议测试使用
`/data/mini_program/shared/test/assessment-data`，生产使用
`/data/mini_program/shared/production/assessment-data`。部署前用实际 systemd 服务用户创建目录，
确认其可创建文件和子目录，并把该目录加入持久化备份；不要把测试和生产指向同一路径。
目录不存在时服务会在首次访问量表时尝试创建，权限不足不会阻止 uvicorn 启动，但量表接口会失败。
量表目录与数据库是两套独立存储，必须分别备份；根健康检查为 `200` 也不能替代量表专项冒烟。

通用上传建议测试使用 `/data/mini_program/shared/test/uploads`、生产使用
`/data/mini_program/shared/production/uploads`。量表图片建议测试使用
`/data/mini_program/shared/test/assessment-assets`、生产使用
`/data/mini_program/shared/production/assessment-assets`。两个图片目录都必须由实际 systemd
服务用户创建并保持可写。量表图片使用内容哈希命名，可能被历史发布版本和报告快照长期引用，
不得在普通发版或清理发布目录时删除。

启动：

```bash
# 测试可单进程；生产建议多 worker
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

---

## 5. 前端 / 管理端

**小程序 `frontend`**

- 构建前把 API 指到本环境：`VITE_API_V2_BASE_URL=https://本环境域名`
- `pnpm build:mp-weixin` → 产物 `dist/build/mp-weixin` → 微信开发者工具上传
- 微信后台配置 request / uploadFile 等**合法域名**（生产不可依赖「不校验域名」）

**admin-web（可选）**

- `NEXT_PUBLIC_API_BASE_URL=https://本环境域名`
- `NEXT_PUBLIC_EAP_BASE_URL=https://EAP站点域名`，用于报告快照中的 EAP 图片
- 看板等管理接口前缀：`/api/mini/admin`（含 `/boards/*`）

**EAP_front_site（可选）**

- `NEXT_PUBLIC_API_BASE_URL=https://API域名`
- `API_INTERNAL_BASE_URL=http://后端内网地址:8000`（无内网地址时可与公开地址相同）
- `NEXT_PUBLIC_DATA_SOURCE=mock`；当前通用内容 `HttpAdapter` 尚未实现，不能设置为 `http`

---

## 6. 推荐上线顺序

### 测试环境

1. 建测试库 → 配测试 `.env` 和独立 `ASSESSMENT_DATA_DIR` → `ensure_schema.py`
2. `migrate_assessment_tables.py --preflight` → 确认测试库并获批 → `--apply --confirm-database <测试库名>`
3.（可选）执行 `seed_demo_data.py`；起后端 + HTTPS 反代 → 冒烟登录、核心业务、量表提交/报告/扫码
4. 构建小程序 → 上传体验版 → 配测试合法域名
5.（可选）部署 admin-web / EAP 并联调

### 生产环境

1. 测试验收通过 → 备份现有库和生产量表目录 → 建/升级生产库 → `ensure_schema.py`（**不灌演示 seed**）
2. 对生产库单独 `--preflight` → 确认并获批 → `--apply --confirm-database <生产库名>`
3. 生产 `.env`（真实微信/支付/短信/JWT、独立量表密钥/目录）→ 多 worker 托管 + HTTPS
4. 构建 admin-web / EAP / 小程序并指向生产 API → 上传或发布
5. 上线冒烟：真实登录、支付回调、上传、量表提交/报告/扫码、管理端关键路径

---

## 7. 发版检查（短清单）

- [ ] 本次是否新增 `App*` 表/列（对照 `models.py`、`ensure_schema.py` 和受控迁移脚本）
- [ ] 测试库已跑通 `ensure_schema`；量表三表已单独预检、获批、迁移并校验
- [ ] `ASSESSMENT_DATA_DIR` 按环境隔离、服务用户可写且已纳入备份
- [ ] `ASSESSMENT_SHARE_SECRET` 已配置为本环境独立密钥，EAP 扫码地址为公网 HTTPS
- [ ] 生产 `.env` 与合法域名、支付回调 URL 正确  
- [ ] 未在生产执行清表 seed  
- [ ] 线上后端为当前代码（无旧 uvicorn 占端口）  
- [ ] 小程序 API 地址为对应环境 HTTPS  

更细的本地开发说明见 `使用文档.md`；后端启动摘录见 `backend-python/DEPLOY.md`。
